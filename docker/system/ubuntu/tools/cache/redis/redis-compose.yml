
services:
  redis:
    container_name: redis
    image: ${PROJECT_NAME}-${CONFIG_SYSTEM}-redis-${VERSION}:latest
    build: .
    profiles:
    - all
    - backend
    - redis
    environment:
      - ALLOW_EMPTY_PASSWORD=yes
    networks:
      - proxy
      - host_network